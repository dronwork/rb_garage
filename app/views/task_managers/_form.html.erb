<%= form_with(model: task_manager, local: true) do |form| %>
  <% if task_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_manager.errors.count, "error") %> prohibited this task_manager from being saved:</h2>

      <ul>
      <% task_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :task_manager_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
